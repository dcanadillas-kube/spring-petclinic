buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: maven
        # replace: true
        stages:
          - name: Test stage
            steps:
              - command: echo
                args:
                  - "Hello David"
              - command: mvn
                args:
                  - -version 
