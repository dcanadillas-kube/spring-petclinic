buildPack: none
pipelineConfig:
  pipelines:
    release:
      pipeline:
        agent:
          image: maven
        # replace: true
        stage:
          - name: Test stage
          steps:
            - command: echo
            - args:
              - "Hello David"
            - command: mvn
              - -version 
